[project]
name = "tringa-test"
version = "0.1.0"
description = "Test repository for tringa"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.2",
]

[tool.pytest.ini_options]
testpaths = ["tests.py"]
